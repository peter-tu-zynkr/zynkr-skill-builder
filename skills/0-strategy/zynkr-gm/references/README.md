# references/ — runtime reading for zynkr-gm

| File | Read when |
|---|---|
| `source-map.md` | step 0 of every run — which doc/tab/section is authoritative, precedence, ⛔ deprecated paths, how to read big docs |
| `derived-state-rules.md` | `progress` step — ENDS_SOON / OVERDUE / UNDATED / STALLED / PROPOSE_DONE / DIRECTION_UNLABELLED, priority weighting (mirrors `scripts/derive_state.py`) |
| `kpi-map.md` | `kpi` step — 19 KPI Dashboard rows, AUTO/SEMI/HUMAN, fetch sketches, runway rule, write rule |
| `lob-skills-seed.md` | `learn` step — LOB → instrument-skills annotation + gaps (not a registry) |
| `config.example.json` + `config.README.md` | first install — schema of the private `~/.config/zynkr/gm.json`; never commit the real one |
| `brief-template.md` · `routine-prompt.tmpl` | authored separately — weekly/monthly brief shape and the cloud-routine prompt template |

No file here contains a real Drive ID, trigger ID or email; all resolve through the private config.
