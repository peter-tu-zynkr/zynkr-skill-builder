# Database

Database work has not started yet.

Near-term plan:

- Use Google Sheets as the editable source of truth
- Normalize the data in the backend
- Add a database once filtering, sync, or admin requirements outgrow a direct Sheets-backed API

When that happens, this folder should hold:

- schema definitions
- migration files
- seed or sync scripts
