# Action Items

This file previously contained a newer working draft than `ACTION-ITEMS.md`, but it is no longer the canonical source.

Use `ACTION-ITEMS.md` at the repo root as the single maintained task list.
